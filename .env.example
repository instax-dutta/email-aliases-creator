# Cloudflare Email Routing Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ============================================================================
# MULTI-DOMAIN SUPPORT
# ============================================================================
# For multiple domains, create separate .env files:
#   .env.domain1-com  (for domain1.com)
#   .env.domain2-com  (for domain2.com)
# Then symlink: ln -sf .env.domain1-com .env
# Output files are named by domain: domain1-com.json, domain2-com.txt, etc.

# ============================================================================
# CLOUDFLARE CREDENTIALS
# ============================================================================

# Required: Cloudflare API Token
# 
# FOR SINGLE DOMAIN:
#   Dashboard → Profile → API Tokens → Create Token
#   Use template: "Edit zone Email Routing Routes"
#
# FOR MULTIPLE DOMAINS (RECOMMENDED):
#   Create Custom Token with permissions:
#   - Zone → Email Routing Routes → Edit
#   - Zone → Zone → Read
#   Account Resources: All zones (or select specific ones)
#
# Get it from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=your_api_token_here

# Required: Zone ID for your domain
# Find it in: Dashboard → Select Domain → Overview → API section (right sidebar)
CLOUDFLARE_ZONE_ID=your_zone_id_here

# Required: Domain where aliases will be created
# Must have Email Routing enabled in Cloudflare
# Examples: example.com, mydomain.io, yoursite.app
EMAIL_DOMAIN=yourdomain.com

# Required: Email address where all aliases will forward to
# This is your actual inbox where emails will be delivered
DESTINATION_EMAIL=your-email@example.com

# ============================================================================
# OPTIONAL SETTINGS
# ============================================================================

# Delay between API requests in milliseconds (default: 100)
# Increase if you hit rate limits
REQUEST_DELAY_MS=100

# Seed for reproducible random name generation
# Use the same seed to generate the same set of aliases
# If not set, uses current timestamp for true randomness
# RANDOM_SEED=12345

# ============================================================================
# INTERACTIVE MODE
# ============================================================================
# The script now runs in INTERACTIVE mode!
# It will ask you to:
#   1. Choose a themed word bundle (Privacy Guardian, Tech Wizard, etc.)
#   2. Enter how many aliases to create (1-500)
